class House {
  constructor(
    id,
    name,
    lng,
    lat,
    viewing_time,
    viewing_date,
    house_information
  ) {
    (this.id = id),
      (this.name = name),
      (this.home_longitude = home_longitude),
      (this.home_latitude = home_latitude);
  }
}
